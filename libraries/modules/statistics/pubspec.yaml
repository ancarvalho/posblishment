name: statistics
description: statistics module.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  dependencies:
    path: "../../dependencies"
  core:
    path: "../../core"
  design_system:
    path: "../../design_system"
  internal_database:
    path: "../../internal_database"



  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

flutter:
  uses-material-design: true


  module:
    androidX: true
    androidPackage: com.example.dashboard
    iosBundleIdentifier: com.example.dashboard
