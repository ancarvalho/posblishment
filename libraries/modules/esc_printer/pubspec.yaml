name: esc_printer
description: A new Flutter module project.
publish_to: none
version: 1.0.0+1

environment:
  sdk: '>=2.19.2 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  dependencies:
     path: "../../dependencies"
  design_system:
    path: "../../design_system"
  core: 
    path: "../../core"
  printer: 
    path: "../../printer"

  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:
  uses-material-design: true

  # To add Flutter specific assets to your application, add an assets section,
  # like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  module:
    androidX: true
    androidPackage: com.example.esc_printer
    iosBundleIdentifier: com.example.escPrinter
