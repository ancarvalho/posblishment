name: design_system
description: Project design system.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: '>=2.19.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  
  flutter:
    sdk: flutter
  dependencies:
    path: "../dependencies"
  core:
    path: "../core"

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
