name: core
description: project core.
version: 0.0.1
publish_to: "none"

environment:
  sdk: '>=2.19.2 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  dependencies:
    path: "../dependencies"

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.2.1
  
  flutter_lints: ^2.0.0
  objectbox_generator: any
  
flutter:
